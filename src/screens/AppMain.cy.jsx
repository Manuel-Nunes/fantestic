import React from 'react'
import AppMain from './AppMain'

describe('<AppMain />', () => {
  beforeEach('renders', () => {
    cy.mount(<AppMain />)
  });

  it('adds a task', () => {
    
  });

  it('completes a task', () => {
    
  });

  it('cancels a task', () => {
    
  });

  it('removes a task', () => {
    
  });
})